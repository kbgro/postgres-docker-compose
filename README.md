# Postresql and pgadmin4 setup with docker

## Requirements:
- Docker
- Dcoker-compose
## run
```bash
$ git clone https://github.com/kbgro/postgres-docker-compose
```
setup the `.env` files. Then run
```bash
$ docker-compose up
```
